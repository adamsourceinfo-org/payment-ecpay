# dev 的金鑰其實公開在綠界文件上，但照樣走 Secret Manager ——
# 兩個環境的載入路徑要一模一樣，不能等 prod 上線才第一次跑到讀 secret 的程式碼。
ECPAY_HASH_KEY=payment-ecpay-hash-key-dev:latest
ECPAY_HASH_IV=payment-ecpay-hash-iv-dev:latest

# 事件推送。**缺席時服務照常啟動，只是推送關閉**（/health 的 push 會說）——
# 第一次部署時 secret 還沒建，硬性必填會讓服務起不來，
# 而沒有推送的服務仍然是完全可用的服務（GET /v1/events 還在）。
WEBHOOK_SIGNING_KEY=payment-ecpay-webhook-signing-key-dev:latest
INTERNAL_KEY=payment-ecpay-internal-key-dev:latest
